# Set to false once the Spring Boot backend (see /backend) is running,
# and the frontend will call it via axios instead of the mock data layer.
VITE_USE_MOCK=true

# Base URL for the Spring Boot API (only used when VITE_USE_MOCK=false).
VITE_API_BASE_URL=http://localhost:8080/api
